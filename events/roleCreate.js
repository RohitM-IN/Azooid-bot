const Discord = require('discord.js');
module.exports = async (role,channel) => {
		
		// var messagechannel = channel.guild.channels.find(ch => ch.name.includes('member-log')) || channel.guild.channels.find(ch => ch.name.includes('log')) || channel.guild.channels.find(ch => ch.name.includes('logs')) || channel.guild.channels.find(ch => ch.name.includes('general')) ;
		// // not getting id name hexColor
		// if (!messagechannel) return;
		
		// const embed = new Discord.RichEmbed()
		// 	.setColor('GREEN')
		// 	.setTimestamp()
		// 	.setAuthor(`:information_source: Role created!`)
		// 	.addField(`📎 RoleID:`, role.id)
		// 	.addField(`🔰 Hexcolor:`, role.hexColor)
		// 	.addField(`📝 Name:`, role.name);
		// messagechannel.send(embed);
}

